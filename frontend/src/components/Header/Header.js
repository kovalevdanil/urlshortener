import React from 'react'

import './Header.css'

const Header = (props) => {
    return <div className = 'header'>
        <div className = 'header__link'>UrlShortener</div>
    </div>
}

export default Header